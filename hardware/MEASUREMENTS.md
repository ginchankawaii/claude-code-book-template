# 実測チェックシート（印刷前に必ず埋める）

筐体CADには **Web で裏が取れなかった仮定値** が残っている（README §9.1）。
3Dプリントは「時間もフィラメントも戻ってこない」ので、**印刷前にこの6項目を実測し、
CAD のパラメータを確定させてから** `cad/mindclip_case.py` を再実行すること。

記入したら数値を `cad/mindclip_case.py` に反映 → 全6構成を再実行 → STL を再生成する。

| # | 測るもの | 設計値（仮定） | 許容範囲 | 実測値 | CAD の変数 |
|---|---|---|---|---|---|
| M1 | **XIAO スタック総厚**（メイン基板＋Sense拡張ボード、カメラを外した状態。基板下面〜拡張ボード上面） | 7.5 mm | allday は 9.5 まで余裕あり／slim はこの値が奥行きを直接決める | ____ mm | `XIAO_STACK_T` |
| M2 | **USB-C コネクタの中心高さ**（スタック下面 → USB-C 開口の中心） | 3.2 mm（CAD は `USB_Z0 = 2.4 + XIAO_LIFT` の 2.4 がこの値の下端側） | **±0.8 mm**（超えるとケーブルが挿さらない＝充電不能） | ____ mm | `USB_Z0` の定数 2.4 |
| M3 | **電池の外形3軸**（リード線を除く本体のみ） | 31 × 25 × 8.0 mm | **厚さ 8.5 mm 以下**（超えたら筐体に入らない） | ___ × ___ × ___ mm | `BAT_W/H/T` |
| M4 | **JST 嵌合体の外形3軸**（オス・メスを挿し合わせた状態） | 5.8 × 11.0 × 4.5 mm | 高さ **allday 8.7 / slim 7.0 mm 以下**、長さ **11.6 mm 以下** | ___ × ___ × ___ mm | `HARNESS_JST_XYZ`（採用構成 A-1。基板構成では `J1_BODY_Z`） |
| M5 | **ピグテール電線の被覆外径** | 1.2 mm (AWG28) | **1.3 mm 以下**（太いと XIAO が浮いて M2 がずれる） | ____ mm | （CAD 変更なし・不合格なら細い線に交換） |
| M6 | **スライドスイッチ SS-12D00 の高さ**（本体高さ／レバー突出） | 3.5 / 2.0 mm | レバー 3 mm の個体が流通している。実測値でスロット深さを確認 | ___ / ___ mm | `SW_H` / `SW_LEVER` |

## 補助チェック（ノギス不要）

- [ ] **電池: 黄色テープの下に保護基板（PCM）があるか目視**。無ければ使わない（XIAO 側に過放電保護は無い）
- [ ] **電池: テスターで極性確認**。赤 = ＋ / 黒 = −。電圧は 3.7〜4.2V の範囲か
- [ ] **磁石: φ6 × 厚 2.0 mm か**（厚 2.5 / 3.0 mm はポケットに入らない）
- [ ] **USB-C ケーブル: データ線が生きているか**（充電専用ケーブルでは書き込みできない）

## 実測値が設計値とズレたときの手順

1. `cad/mindclip_case.py` の該当変数を実測値に書き換える
2. `GOLDEN_OUTER` が外形の回帰テストとして効いているので、**外形が変わると assert で止まる**。
   これは正常な挙動。変わった外形を `MECHANICAL.md` §1・`README.md` §3 に反映してから
   `GOLDEN_OUTER` を新しい値に更新する
3. 全6構成（allday/slim × none/sense/carrier）を再実行し、干渉チェックが通ることを確認
4. STL を再生成してから印刷する
