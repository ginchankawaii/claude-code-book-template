// =============================================================================
// app.h — 状態機械の本体（実体は app.cpp）
//
// Arduino の .ino プロトタイプ自動生成（ctags）は、
//  ・日本語（マルチバイト）コメントでバイトオフセットがずれる
//  ・ctags 5.9 の typeref 形式差で戻り値型が落ちる（環境によっては setup/loop も壊れる）
// という不安定さがあるため、**関数定義は一切 .ino に置かず**、
// setup()/loop() を含めてすべて .cpp 側（このモジュール）に置いている。
// mindclip.ino はスケッチ名を満たすためだけの入口ファイル。
// =============================================================================
#pragma once
#include "config.h"
