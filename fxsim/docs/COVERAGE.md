# カバレッジ台帳（規律9）

**なぜこれが在るか。** 2026-09-18、ライブが**68時間停止**した。原因はRound-6初日に書き換えた
ロック機構の1分岐で、その後の**10パスの監査は一度もロックを再攻撃しなかった**。全パスが
「最新コード＝当日書いた部分」を叩いており（規律6）、対抗軸が無かった。
同じ理由で、**人が増額を決める根拠**である `run_monitor` の🟢も10パス目まで未攻撃だった。

台帳の目的はひとつ：**「一度も攻撃していない」を、レンズの沈黙から推測するのではなく空欄として
見えるようにする。** 監査のたびに更新し、完全性批評家に最新版を作らせること。

## 不変条件

| | 内容 |
|---|---|
| I1 | ショート注文は決して出ない |
| I2 | ノーションは5倍レバ上限を超えない |
| I3 | 開いているロングには常に生きた保護ストップがある。無ければ心拍を止めてEAのEXPフェイルセーフが有限時間で決済する |
| I4 | FLAT意図は有限時間で建玉0に収束する |
| I5 | 古い/異常なバーで新規注文を出さない |
| I6 | 脳はスーパーバイザが再起動ループを起こす形で終了しない |
| I7 | 新しい判断なしに注文が（再）発行されない |
| I8 | `steady_signal.txt` を書くプロセスはただ1つで、全ライターがSEQ/EXP/SLを話す |
| I9 | 公開ダッシュボードから注文・ストップ・シグナル・DBを変更できない |
| I10 | 運用者の健康信号は、壊れているのに健全と表示しない／健全なのに壊れていると表示しない |
| **I11** | **システムが入りうるすべての状態に有限の脱出口がある。** 分岐・待機・バックオフ・フラグ・ループが無期限に保持しない。保持が正しい場合（本当に生きた先住）は、計器がそう言い、対処を名指しする（Round-7新設） |

## 台帳（最終更新：Round-7 / 2026-09-18）

P1〜P8 = Round-6の各パス、R2〜R5 = 各ラウンド、**R7 = Round-7**、
**`never` = どのパスの記述もそのセルを名指ししていない**（「たぶん含まれる」は `never` と書く）、
`–` = そのコンポーネントに適用されない。

| コンポーネント（ライブ経路） | I1 | I2 | I3 | I4 | I5 | I6 | I7 | I8 | I9 | I10 | I11 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| run_ai_bridge: 常駐状態機械 | P8 | P8 | P8 | P8 | P8 | P5 | P8 | P1 | – | P8 | R7 |
| run_ai_bridge: ロック | – | – | – | – | – | P1 | – | **R7** | – | **R7** | **R7** |
| run_ai_bridge: decide_once/サイジング | P8 | R4 | P8 | – | P8 | – | P8 | – | – | **R7** | `never` |
| run_ai_bridge: _restore_state/_find_run | – | – | P1 | – | – | – | P1 | – | – | P8 | `never` |
| mt5_ea/SteadyBridge.mq5 | P4 | P6 | P5 | P6 | – | – | P5 | P6 | – | P3 | P6 |
| app/bridge.py | – | – | P5 | P5 | P5 | – | P5 | P1 | – | P6 | P6 |
| app/monitor.py + run_monitor | – | – | – | – | – | – | – | – | – | **R7** | **R7** |
| scripts/diagnose_live.py | – | R4 | – | – | – | – | – | – | – | P8 | R7 |
| app/server.py（公開ダッシュボード） | – | – | – | – | – | – | – | P1 | P1 | **R7** | **R7** |
| app/db.py | – | – | – | – | – | **R7** | – | – | `never` | **R7** | **R7** |
| app/config.py（env解析） | – | **R7** | – | – | – | `never` | – | – | – | **R7** | `never` |
| app/sizing.py | – | R4 | – | – | – | – | – | – | – | **R7** | – |
| app/ai_trader.py | – | R4/**R7** | – | – | – | R4 | – | – | – | **R7** | R4 |
| app/providers/csv.py（バー解析） | – | – | – | – | R4/**R7** | `never` | – | – | – | `never` | `never` |
| app/indicators.py（ATR→ストップ/サイズ） | – | `never` | `never` | – | `never` | – | – | – | – | `never` | – |
| app/events.py（カレンダー→トリガ） | – | – | – | – | `never` | – | `never` | – | – | `never` | **R7** |
| app/adaptive.py（DDブレーキ・既定OFF） | – | R4 | – | – | – | – | – | – | – | `never` | **R7** |
| docker-compose / Dockerfile / .env | – | **R7** | – | – | – | **R7** | – | `never` | P1 | **R7** | `never` |
| app/models.py, providers/base.py | – | – | – | – | – | – | – | – | – | `never` | – |

**ライブ経路外**（fx/dashboardコンテナが使わない、または起動を拒否する）：
`app/live.py` `app/mt5_live.py` `app/steady*.py` `app/backtest.py` `app/strategies/*`
`app/fundamental/*` `app/resample.py` `app/ai_stock_trader.py`
`scripts/run_bridge.py`（脳のロックが新鮮な間は起動拒否・P1） `scripts/research_run*.py`

### 台帳が示したこと
**I11の列がほぼ全面 `never`** ——ロックは孤立した見落としではなく、**誰も列を持っていなかった
クラスの最初の1例**だった。`never` はさらに config/env解析・db.py・docker/.env・indicators・
events に集中している。規律6（最新コードを叩け）だけで回すと、**変更されないコードは永遠に
攻撃されない**。

## 攻撃済み・欠陥ゼロ（Round-7 が確認し、次回の台帳で R7 と書けるもの）
- **常駐ループのI11全掃討**：`settle_pending`（初回の信頼プローブで解除、それまで心拍は保留＝
  フェイルセーフが生きる）／`veto_until` +1h／`entry_backoff_until`（R7で逓増化）／`last_gate` 15分／
  `blind_grace` 3×poll→心拍保留／`torn_row`は行ごとに再計算。すべて有限。
  `intent==FLAT & pos>0` だけはEAが閉じられない限り無限だが、`執行一致` が🔴で
  「EAのアルゴ取引ON/エラーを確認」と対処を名指しするのでI11の条件を満たす（反証済み）。
- **`app/db.py`**：signalsの主キー衝突なし（同一 `now` を共有する分岐は相互排他）、
  ISO-8601のTEXT順序は時系列順、sqliteの5秒タイムアウトは例外を上げるので無限待ちなし。
- **`app/providers/csv.py`**：MT5の `TimeToString` 形式と一致、区切り推定OK、間隔検査は3600sで通過。
  `steady_bars.csv` は**非原子的**（tmp+FileMoveはstatusだけ）だが、切断は必ず前方一致なので
  「本数不足」か「古すぎ」で1ポーリング盲目になるだけ。自己回復。
- **`app/events.py`**：有料リフレッシュは試行ベースで12h制限、呼び出し前に刻印、失敗はキャッシュへ退避。
- **`app/sizing.py` / `size_lots`**：上限超過なし、ロットステップ切り捨て、最小ロット未満は0。
- **`app/server.py` I9**：`/api/backtest` はcsv|sampleのみ・20,000本上限・逐次化・設定はリクエスト毎コピー。

## 次のパスが最初に見るべき空欄（金額リスク順）
1. **実機のMQL5とWindows共有** — EA側の全閉鎖証明はPython転写経由。`os.replace` / `FileMove` が
   Dockerのbind mount上で本当に原子的かは、所有者のPCでしか確かめられない。
2. **`app/indicators.py`** — ATRがストップ幅とサイズの両方を決めるのに、I2/I3/I5のどれでも未攻撃。
3. **`requirements.txt` が完全に未固定**（`pandas>=2.0` 等）で、`docker compose up -d --build` が
   毎回解決し直す。ライブマネー経路の再現性リスク。
4. **年末の長期休場** — 実テープ596週末のうち3回が `MAX_BAR_AGE_H=75h` を超える（76/78/79h）。
   79hだと健全なロングがフェイルセーフ決済の約30分手前まで来る。今日の欠陥ではないが余裕が薄い。
5. **`app/config.py` の I6/I11** と **docker/.env の I8/I11**。

## 運用点の注意（欠陥ではないが知っておくこと）
USD/JPY 150・shadow権限（確信度0.6固定）では、**どちらの制約が効くかは残高ではなく ATR で決まる**。
リスク基準ロット `B·r/stop` とレバ基準ロット `L·B/P` は**どちらも残高Bに比例する**ので、比は
残高に依存しない：`(r/stop)/(L/P)`。実測（`size_lots`、risk 4%・cap 5x）：

| ATR | 効く制約 | 27.2万円 | 70万円 | 100万円 |
|---|---|---|---|---|
| 0.20 | レバ | 0.09 | 0.23 | 0.33 |
| 0.30 | レバ | 0.09 | 0.23 | 0.33 |
| 0.50 | リスク% | 0.08 | 0.22 | 0.32 |

したがって**平時（ATR≲0.45）は `max_risk`・DDブレーキ・確信度が建玉サイズを一切変えない**。
高ボラ時だけリスク%が効き始める。1取引あたりの損失はどの残高でも**残高の1.0〜2.4%**で一定。
Round-7でログに「どの制約がサイズを決めたか」を表示するようにした。
（以前この欄に「残高30万円を超えると関係が変わる」と書いていたのは**誤り**。0.10ロットという
境界はEAのリサイズ不感帯 `InpResizeMinLots` の話で、サイズを決める制約とは無関係だった。）
